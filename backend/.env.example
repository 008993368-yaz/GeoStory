APP_ENV=development
CORS_ORIGINS=http://localhost:5173

# Database Configuration
# PostgreSQL connection string for application runtime (async with asyncpg)
# Format: postgresql+asyncpg://username:password@host:port/database_name
DATABASE_URL=postgresql+asyncpg://ls_user:password@localhost:5432/geostory

# For Alembic migrations, use synchronous connection (handled automatically in alembic/env.py)
# The env.py will convert asyncpg to psycopg2 if needed, or you can set a separate variable:
# MIGRATION_DATABASE_URL=postgresql://ls_user:password@localhost:5432/geostory

# Application Settings
DEBUG=true
